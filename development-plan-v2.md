# 我拼个豆 v2.0 开发计划与验收测试

## 1. 执行边界

本计划仅实现 `prd.md` 第 25 至 31 节的 v2.0 增量，不重构已通过验收的 v1.0 主流程。每个阶段遵循同一门禁：先写或补充该阶段的有效自动测试，执行测试，定位并修复失败原因，测试通过后再创建 Git 提交。不得删除、跳过或降低测试标准以换取通过。

阶段完成提交前均执行：对应测试文件、`ruff check .` 和与改动相关的既有回归测试。V2-Final 再执行全部测试。

## 2. 阶段总览

| 阶段 | 目标 | 新增验收测试 | 完成提交 |
|---|---|---|---|
| V2-0 | 锁定需求、基线与迁移策略 | 全量基线测试 | `docs: define v2 scope and delivery plan` |
| V2-1 | 用户资料、设置、语言与分类数据 | 模型、权限、语言回退、分类迁移测试 | `feat: add v2 profile settings and categories` |
| V2-2 | 模拟会员闭环与张数规则 | 会员升级、额度、候选版本、管理员权限测试 | `feat: add membership demo and quota rules` |
| V2-3 | 正确尺寸与人物面部保护 | 尺寸配置、材料估算、模式、脸部风险测试 | `feat: add production pattern sizes and face modes` |
| V2-4 | 烫豆推荐与制作引导 | 用途推荐、四种方式资料、风险提示测试 | `feat: add ironing recommendation guide` |
| V2-5 | 保存弹窗、图纸库分类交互与四主题适配 | 无刷新保存、失败保留、主题 DOM 测试 | `feat: add themed pattern save modal` |
| V2-6 | API 演示接口与安全边界 | 202、所有权、状态、额度、幂等与攻击限制测试 | `feat: add generation task confirmation api` |
| V2-7 | 全流程回归与作品集验收 | 多语言、会员、创作、保存、API 端到端测试 | `test: complete v2 acceptance coverage` |

## 3. V2-0：需求和测试基线

任务：将确认需求追加到 PRD；建立本计划；记录当前 `main` 和 `v1.0.0` 标签；运行全量既有测试作为 v2 开发基线。

验收：工作区无意外改动；既有测试、Ruff 均通过；文档明确 v1 与 v2 边界。

## 4. V2-1：资料、设置、多语言和分类

任务：

- 扩展用户资料：头像、昵称、邮箱、简介、默认拼豆风头像和密码修改入口；
- 增加用户设置及四语言前台文案；后台维持中文；
- 建立默认分类模板、用户分类及“删除后移入其他”的规则；
- 后台提供新用户默认分类模板配置。

验收测试：

- 用户只能修改自己的资料、设置和分类；
- 新用户得到默认七分类，既有用户分类不因模板改动而被覆盖；
- 删除分类后图纸保留并迁入其他；
- 四语言功能文案可切换，缺失翻译安全回退；
- 设置保存后下次访问仍生效。

## 5. V2-2：会员闭环与张数

任务：

- 实现方案对比、模拟升级、会员中心和后台可配置的会员权益；
- 将会员等级与图纸质量解耦；
- 实现明确提示、预留、成功使用、失败释放和多候选版本的张数规则；
- 使用标准 Django 管理机制提供管理员 Pro 权限，不存储明文密码。

验收测试：

- 四类用户只在张数和附加工具上不同，基础图纸参数不被会员等级降级；
- 升级成功后权益即时生效；
- 失败、取消和重复请求不会多扣张数；
- 生成 N 个候选版本前显示并预留 N 张，成功后准确结算；
- 管理员可以修改配置，普通用户不能。

## 6. V2-3：尺寸体系和人物质量

任务：

- 将可配置尺寸改为十种 29×29 拼板组合，增加 14×14 图标模式；
- 输出拼板数、预计成品大小、用豆数量和适用说明；
- 增加脸部细节优先、整体构图优先、重点人脸选择和低尺寸预警；
- 在确定性转换中加入局部特征保护与结果页面部细节检查。

验收测试：

- 每种尺寸网格、拼板数量、成品尺寸和材料统计一致；
- 29×29 人像会预警但仍可生成；58×58 及以上人像可进入脸部细节模式；
- 人物模式改变特征保护参数而不改变用户选择的尺寸；
- 多人图片可选择重点人脸；
- 结果页能展示面部放大检查和可继续保存的风险提示。

## 7. V2-4：烫豆推荐

任务：

- 实现用途选择与图纸信息组合推荐；
- 建立四种烫豆方式的说明、标准化效果图、材料、步骤与安全提示；
- 在结果和图纸详情页提供推荐入口。

验收测试：

- 五种用途都有可解释的推荐结果和替代方案；
- 四种方式都包含效果图与新手可执行信息；
- 用户可改选方式，系统不阻断；
- 推荐仅使用当前图纸和用途数据，不泄露其他用户内容。

## 8. V2-5：保存弹窗和图纸库体验

任务：

- 用无刷新弹窗替代独立保存页；
- 支持名称、分类、折叠备注、成功反馈、失败保留与图纸库跳转；
- 让分类管理和弹窗在四套主题下共享视觉变量；
- 支持键盘焦点管理和 Esc 关闭。

验收测试：

- 保存请求不导致整页刷新；
- 成功后页面正确更新，失败后表单内容仍在；
- 用户只能保存到自己的分类；
- 四主题下弹窗可打开、关闭、聚焦和保存。

## 9. V2-6：API 演示接口

任务：

- 实现 `POST /api/v1/generation-tasks/{task_id}/confirm/`；
- 完成认证、所有权、状态、额度、预留、异步任务和统一响应；
- 提供状态查询地址和幂等请求处理；
- 记录恶意调用并施加合理的限制。

验收测试：

- 成功请求返回 202 和状态查询地址；
- 未登录、非所有者、非法状态、额度不足均返回准确错误且不创建任务；
- 相同幂等请求不会重复预留或扣减张数；
- 生成失败释放张数；
- 恶意高频或异常请求被限制，普通用户不受影响。

## 10. V2-7：全量验收与交付

任务：

- 增加覆盖“注册／升级／上传／人物模式／尺寸／烫豆推荐／保存／分类／语言／API”的端到端流程；
- 执行全部自动测试、Ruff 和迁移检查；
- 检查迁移前后数据兼容性与 v1 主流程回归；
- 更新测试报告和演示说明。

验收命令：

```bash
uv run ruff check .
uv run pytest
uv run python manage.py makemigrations --check --dry-run
```

若浏览器测试依赖可用，再执行：

```bash
uv run pytest tests/e2e
```

最终交付条件：所有命令通过；每阶段均有独立提交；工作区干净；v1.0 标签保持不动；用户可按演示说明完成完整 v2 流程。
