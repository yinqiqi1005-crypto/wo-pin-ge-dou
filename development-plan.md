# 我拼个豆开发计划

## AI 图片生成拼豆图纸工作流 v1.0

| 项目 | 内容 |
|---|---|
| 依据文档 | `prd.md` |
| 计划状态 | 可执行初版 |
| 开发对象 | 单人开发、Python 入门学习者、AI 辅助开发 |
| 最终目标 | 可完整演示的消费级 AI 拼豆产品 Demo |
| 开发原则 | 先完成确定性图纸核心，再接 AI；每阶段必须测试通过后进入下一阶段 |

---

## 1. 计划结论

项目采用“先核心、后体验、再高级能力”的开发顺序：

```text
工程基础
→ 拼豆转换核心
→ 用户、配置与数据
→ 基础生成闭环
→ 后台任务与生成张数
→ AI 图片分析
→ 六步用户体验
→ Plus/Pro 高级创作
→ 图纸库、PNG/PDF 与运营后台
→ 全量测试和作品集演示
```

不从 AI 模型调用开始。首先证明系统能够把一张普通图片稳定转换为网格、合法颜色和准确材料清单，再逐步增加 AI 分析和高级创作。

## 2. 默认技术方案

PRD 没有锁定技术栈。为兼顾 Python 学习成本、管理后台、用户系统和完整作品集展示，开发计划采用以下默认方案。

### 2.1 后端与页面

- Python；
- Django 单体应用；
- Django 模板负责主要页面；
- HTMX 处理局部刷新和任务状态更新；
- Alpine.js 处理少量前端交互；
- Tailwind CSS 或同类方案负责页面样式；
- Django Admin 作为首版运营配置后台。

选择理由：

- 用户、权限、数据库、表单和管理后台可以使用同一套框架；
- Python 入门者不需要同时维护独立的 React 前端和 Python API；
- 仍然能够实现上传、进度、裁剪、预览和动态页面；
- 后续如果需要独立前端，可以再为 Django 增加 API 层。

### 2.2 数据和后台任务

- PostgreSQL：正式业务数据；
- Redis：后台任务队列和短期状态；
- Celery：图片分析、AI 调用、图纸生成和 PDF 导出；
- 本地文件存储：开发阶段；
- 对象存储适配层：后续部署时可替换为 S3 兼容存储。

### 2.3 图片与文档处理

- Pillow：读取、旋转、缩放、裁剪和 PNG 输出；
- NumPy：网格和颜色数据计算；
- scikit-image 或等价库：颜色空间转换和色差计算；
- ReportLab 或等价库：PDF 制作图输出。

### 2.4 测试和质量

- pytest；
- pytest-django；
- Django Test Client：页面和接口测试；
- Playwright：六步用户流程端到端测试；
- Ruff：代码检查和格式规范；
- GitHub Actions 或同类 CI：自动运行检查和测试。

所有依赖在项目初始化时选择受支持的稳定版本并锁定，避免直接追随未验证的新版本。

---

## 3. 建议项目结构

```text
我拼个豆/
├── prd.md
├── development-plan.md
├── README.md
├── manage.py
├── config/                  # Django 全局设置、路由和任务配置
├── apps/
│   ├── accounts/            # 用户、登录和会员等级
│   ├── memberships/        # 会员权限、周期张数和额度记录
│   ├── creation/           # 六步创作流程和 AI 任务
│   ├── patterns/           # 图纸、色板、网格和材料清单
│   ├── library/            # 我的图纸和作品版本
│   └── operations/         # 运营数据和后台配置
├── services/
│   ├── image_processing/   # 确定性图片与网格算法
│   ├── ai/                 # 模型适配层和 Prompt
│   ├── exports/            # PNG、PDF 导出
│   └── storage/            # 本地与对象存储适配
├── templates/
├── static/
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── e2e/
│   └── fixtures/
├── docs/
│   ├── architecture.md
│   ├── data-model.md
│   ├── ai-prompts.md
│   └── test-report.md
└── docker-compose.yml
```

结构可以随着实现调整，但以下边界必须保留：

- 确定性图片算法不能依赖页面；
- AI 供应商调用不能散落在业务代码中；
- 会员配置不能写死在页面或模型 Prompt 中；
- 图纸数据必须有唯一的正式数据源。

---

## 4. 开发里程碑

| 里程碑 | 目标 | 预计专注开发时间 | 完成后可演示内容 |
|---|---|---:|---|
| M0 | 工程基础和开发环境 | 2～3 天 | 项目启动、测试和管理后台 |
| M1 | 确定性拼豆转换核心 | 6～8 天 | 图片转换为真实网格和材料清单 |
| M2 | 用户、会员、配置和数据模型 | 4～6 天 | 四类用户及后台配置 |
| M3 | 基础生成最小闭环 | 6～8 天 | 上传到保存的基础版本 |
| M4 | 后台任务和生成张数 | 5～7 天 | 异步生成、恢复、预留和释放 |
| M5 | AI 图片分析 | 5～7 天 | 主体、适配分析和参数推荐 |
| M6 | 六步完整用户体验 | 6～8 天 | 完整前台流程和异常状态 |
| M7 | Plus/Pro 高级创作 | 7～10 天 | 图像编辑、二次创作和版本 |
| M8 | 图纸库、导出和运营后台 | 6～8 天 | PNG/PDF、图纸库和运营数据 |
| M9 | 全量测试、优化和演示发布 | 7～10 天 | 可用于求职展示的 v1.0 Demo |

总计约 54～75 个专注开发日。对于 Python 入门学习者，建议按 12～16 周兼职节奏执行，不以压缩时间为目标。

时间是估算，不作为跳过测试或验收条件的理由。每个里程碑以“完成标准”作为进入下一阶段的依据。

---

## 5. M0：工程基础和开发环境

### 5.1 目标

建立可重复启动、可测试、可提交的项目基础。

### 5.2 任务清单

- [ ] M0-01 初始化 Git 仓库并创建 `.gitignore`；
- [ ] M0-02 建立 Python 虚拟环境和依赖清单；
- [ ] M0-03 创建 Django 项目；
- [ ] M0-04 建立 `apps`、`services`、`tests` 和 `docs` 目录；
- [ ] M0-05 配置 PostgreSQL 开发数据库；
- [ ] M0-06 配置 Redis 和 Celery；
- [ ] M0-07 配置环境变量读取，不提交密钥；
- [ ] M0-08 配置 Ruff 和 pytest；
- [ ] M0-09 配置自动测试工作流；
- [ ] M0-10 创建健康检查页面和第一个自动测试；
- [ ] M0-11 编写本地启动和测试说明；
- [ ] M0-12 创建超级管理员并确认 Django Admin 可用。

### 5.3 测试

- 应用可以正常启动；
- 数据库迁移可以成功执行；
- Redis 和任务工作进程可以连接；
- 健康检查返回成功；
- 自动测试和代码检查通过；
- 全新环境按 README 可以重新启动。

### 5.4 完成标准

- 任何后续开发都可以通过统一命令启动和测试；
- 仓库中没有密钥、个人绝对路径和临时文件；
- 至少有一条 CI 检查成功运行。

### 5.5 建议原子提交

- `chore: initialize django project`
- `chore: configure database and task queue`
- `test: add project health check`
- `docs: add local development guide`

---

## 6. M1：确定性拼豆转换核心

### 6.1 目标

不接任何 AI 模型，先完成“输入图片—正式拼豆数据—PNG 预览”的核心能力。

### 6.2 数据设计任务

- [ ] M1-01 定义色板文件格式；
- [ ] M1-02 建立通用标准色板及内部颜色编号；
- [ ] M1-03 定义正式网格数据结构；
- [ ] M1-04 定义空白格和非空格状态；
- [ ] M1-05 定义材料清单数据结构；
- [ ] M1-06 确保同一份正式网格同时驱动预览和材料统计。

### 6.3 图片算法任务

- [ ] M1-07 读取 JPG、PNG 并处理 EXIF 方向；
- [ ] M1-08 校正透明背景和色彩模式；
- [ ] M1-09 按正方形比例执行居中裁剪；
- [ ] M1-10 将图片缩放到 30×30、50×50 和 70×70 网格；
- [ ] M1-11 将每个网格颜色映射到启用色板；
- [ ] M1-12 支持 12 色、24 色和 36 色档位；
- [ ] M1-13 统计每种颜色数量和总用豆数量；
- [ ] M1-14 生成无网格效果预览；
- [ ] M1-15 生成带网格和颜色编号的 PNG；
- [ ] M1-16 实现最小版本的孤立格检测和清理；
- [ ] M1-17 实现技术硬性校验器。

### 6.4 必须编写的测试

- 不同方向 JPG 能够正确旋转；
- 透明 PNG 能够正确识别空白区域；
- 三种尺寸输出宽高准确；
- 每个非空格颜色都存在于色板；
- 实际颜色数量不超过设置；
- 各颜色数量之和等于非空格数量；
- 总用豆数量等于非空格数量；
- 相同输入和相同参数重复运行结果一致；
- 损坏文件、空文件和超小图片得到明确错误；
- PNG 预览与正式网格数据一致。

### 6.5 完成标准

- 可以通过测试或内部命令输入一张图片并生成正式网格、PNG 和材料清单；
- 技术数据一致性达到 100%；
- 核心算法不依赖数据库、页面和 AI 服务；
- 至少准备 10 张图片完成第一轮算法对比。

### 6.6 建议原子提交

- `feat: add standard bead palette`
- `feat: normalize uploaded images`
- `feat: convert images to bead grids`
- `feat: generate material counts`
- `feat: render pattern previews`
- `test: validate pattern data consistency`

---

## 7. M2：用户、会员、配置和数据模型

### 7.1 目标

建立支持四类用户、后台配置和作品版本的业务数据基础。

### 7.2 用户与会员任务

- [ ] M2-01 使用 Django 用户系统实现注册、登录和退出；
- [ ] M2-02 定义游客、注册会员、Plus、Pro 四类层级；
- [ ] M2-03 定义会员方案配置；
- [ ] M2-04 定义周期生成张数；
- [ ] M2-05 定义功能权限配置；
- [ ] M2-06 定义会员有效期和额度恢复时间；
- [ ] M2-07 在 Django Admin 中支持编辑会员配置；
- [ ] M2-08 创建默认演示配置，但不在代码中写死业务判断。

### 7.3 核心数据模型任务

- [ ] M2-09 建立 AI 生成任务模型；
- [ ] M2-10 建立图片分析结果模型；
- [ ] M2-11 建立生成参数模型；
- [ ] M2-12 建立图纸和作品版本模型；
- [ ] M2-13 建立色板及颜色模型；
- [ ] M2-14 建立生成张数记录模型；
- [ ] M2-15 建立模型调用和成本记录模型；
- [ ] M2-16 建立配置变更记录；
- [ ] M2-17 为关键关系增加数据库约束和索引；
- [ ] M2-18 编写数据模型说明文档。

### 7.4 必须编写的测试

- 四类用户能够正确识别；
- 权限来自配置而不是硬编码；
- 配置变化只影响新任务；
- 作品不能关联到错误用户；
- 一个作品可以拥有多个版本；
- 额度记录不能导致剩余张数小于零；
- 重复请求不能生成重复额度记录。

### 7.5 完成标准

- 管理员可以在后台修改会员张数和权限；
- 所有核心数据对象可以在后台查看；
- 数据约束能够阻止明显错误状态；
- 自动测试覆盖用户隔离和额度底线。

---

## 8. M3：基础生成最小闭环

### 8.1 目标

实现第一个端到端可用版本：注册用户上传图片、选择基础参数、生成图纸并保存。

此阶段先使用规则分析或模拟分析结果，不接真实 AI。

### 8.2 页面任务

- [ ] M3-01 创建基础导航和创作入口；
- [ ] M3-02 创建图片上传页面；
- [ ] M3-03 创建上传文件校验和错误提示；
- [ ] M3-04 创建图片预览和重新上传；
- [ ] M3-05 创建模拟分析结果页面；
- [ ] M3-06 创建设置页；
- [ ] M3-07 支持 30×30、50×50、70×70；
- [ ] M3-08 支持颜色档位；
- [ ] M3-09 支持背景保留和基础空白处理；
- [ ] M3-10 创建同步生成流程；
- [ ] M3-11 创建效果、网格和材料三个结果标签；
- [ ] M3-12 创建作品命名和保存；
- [ ] M3-13 创建基础“我的图纸”列表；
- [ ] M3-14 创建图纸详情页。

### 8.3 服务任务

- [ ] M3-15 建立上传文件存储服务；
- [ ] M3-16 将 M1 图纸核心接入业务流程；
- [ ] M3-17 创建正式图纸和作品版本；
- [ ] M3-18 保存生成参数和材料清单；
- [ ] M3-19 确保刷新页面后可以重新读取结果。

### 8.4 必须编写的测试

- 合法图片可以上传；
- 非法文件不能进入生成流程；
- 参数可以正确传递给图纸核心；
- 结果页面数据与图纸核心一致；
- 保存后能在“我的图纸”中查看；
- 用户无法查看其他用户的作品；
- 保存失败不会破坏已经生成的结果。

### 8.5 完成标准

- 注册用户可以完成基础生成闭环；
- 图纸可以刷新恢复并保存；
- 页面不依赖真实 AI 也能完整演示；
- 这一版本作为后续 AI 集成的稳定基线。

### 8.6 第一个可演示版本

M3 完成后，应录制一次内部演示：

```text
登录
→ 上传宠物图片
→ 选择 50×50 和 24 色
→ 生成图纸
→ 查看材料清单
→ 保存到我的图纸
```

---

## 9. M4：后台任务和生成张数

### 9.1 目标

将生成流程改造成可恢复的后台任务，并正确处理张数预留、使用和释放。

### 9.2 任务状态任务

- [ ] M4-01 定义正常任务状态和允许的状态变化；
- [ ] M4-02 定义上传、分析、额度、生成、校验和保存错误码；
- [ ] M4-03 将图纸生成迁移到 Celery 任务；
- [ ] M4-04 保存每个后台阶段的进度；
- [ ] M4-05 前台轮询或订阅任务状态；
- [ ] M4-06 用户刷新或离开后恢复任务；
- [ ] M4-07 排队任务支持取消；
- [ ] M4-08 已执行任务不开放强制取消。

### 9.3 生成张数任务

- [ ] M4-09 检查当前会员权限；
- [ ] M4-10 检查周期剩余张数；
- [ ] M4-11 在数据库事务中预留一张；
- [ ] M4-12 任务成功后正式使用一张；
- [ ] M4-13 系统失败后释放预留；
- [ ] M4-14 自动重试不重复预留；
- [ ] M4-15 重复点击使用同一个任务幂等键；
- [ ] M4-16 参数型调整不使用新张数；
- [ ] M4-17 内容型新版本成功后使用一张。

### 9.4 必须编写的测试

- 两次快速点击只创建一个任务；
- 两个并发任务不能超出剩余张数；
- 成功任务只使用一张；
- 自动重试不重复使用；
- 任务失败后剩余张数恢复；
- 排队取消后释放预留；
- 用户离开页面后任务继续；
- 任务状态和额度记录始终一致；
- 剩余张数不会出现负数。

### 9.5 完成标准

- 所有生成操作通过后台任务执行；
- 额度错误扣减测试为 0；
- 用户能够在任务记录中看到排队、生成、成功和失败；
- 任务失败能够定位到具体处理阶段。

---

## 10. M5：AI 图片分析

### 10.1 目标

用真实多模态能力替换模拟分析结果，但保留规则降级方案。

### 10.2 模型选型任务

- [ ] M5-01 选择 2～3 个候选多模态模型；
- [ ] M5-02 使用同一组 10～15 张图片比较主体识别、结构化输出、中文说明、延迟和成本；
- [ ] M5-03 记录选型结果和理由；
- [ ] M5-04 选择首选模型和一个降级模型；
- [ ] M5-05 模型名称和路由通过后台配置。

### 10.3 模型适配层任务

- [ ] M5-06 定义统一图片分析服务接口；
- [ ] M5-07 实现真实供应商适配器；
- [ ] M5-08 实现本地模拟适配器；
- [ ] M5-09 实现超时、有限重试和错误分类；
- [ ] M5-10 记录模型、Prompt、耗时和内部成本；
- [ ] M5-11 对模型输出进行字段和枚举校验；
- [ ] M5-12 模型失败时使用规则默认值和手动裁剪。

### 10.4 Prompt 任务

- [ ] M5-13 创建图片分析 Prompt v1.0；
- [ ] M5-14 固定主体、质量、适配和推荐字段；
- [ ] M5-15 防止图片内文字覆盖系统指令；
- [ ] M5-16 限制模型只能推荐后台已启用选项；
- [ ] M5-17 保存 Prompt 版本；
- [ ] M5-18 建立 Prompt 回归测试样例。

### 10.5 主体分割任务

- [ ] M5-19 评估专用主体分割能力；
- [ ] M5-20 在需要去背景或多主体选择时按需调用；
- [ ] M5-21 保存主体蒙版；
- [ ] M5-22 用户可重新选择或裁剪主体；
- [ ] M5-23 分割失败时允许保留背景继续生成。

### 10.6 必须编写的测试

- 单人、宠物和普通物品主体识别正确；
- 多主体时能够提示用户确认；
- 低可靠结果不会伪装成确定答案；
- 模型返回缺失或非法字段时能够降级；
- 模型超时不会扣减生成张数；
- 图片中的文字不会改变系统任务；
- 同一图片和裁剪结果能够复用分析；
- 只修改尺寸或颜色不会重复调用分析模型。

### 10.7 完成标准

- 智能分析页面使用真实分析结果；
- 模型不可用时基础流程仍能继续；
- 推荐参数只来自后台已启用配置；
- 分析结果、Prompt 版本和调用成本可追踪。

---

## 11. M6：六步完整用户体验

### 11.1 目标

完成 PRD 中六个前台阶段和 22 个必要页面状态。

### 11.2 上传与分析体验

- [ ] M6-01 完成拖拽和点击上传；
- [ ] M6-02 显示格式、大小和优质图片提示；
- [ ] M6-03 显示分析阶段反馈；
- [ ] M6-04 显示主体区域；
- [ ] M6-05 显示适合、可以尝试、不太适合和无法处理；
- [ ] M6-06 显示最多三个主要问题；
- [ ] M6-07 支持重新选择主体；
- [ ] M6-08 风险状态保留“仍然继续”。

### 11.3 设置体验

- [ ] M6-09 默认填入系统推荐参数；
- [ ] M6-10 基础选项保持简洁；
- [ ] M6-11 按会员配置展示高级选项；
- [ ] M6-12 显示会员等级和剩余张数；
- [ ] M6-13 明确本次将使用几张；
- [ ] M6-14 额度不足时保留当前设置；
- [ ] M6-15 阻止明显冲突的参数组合。

### 11.4 生成和结果体验

- [ ] M6-16 显示五个可理解的生成阶段；
- [ ] M6-17 显示自动重试且说明不重复计数；
- [ ] M6-18 允许离开页面并从任务记录返回；
- [ ] M6-19 完成效果、网格和材料标签；
- [ ] M6-20 实现网格放大查看；
- [ ] M6-21 显示制作难度和建议；
- [ ] M6-22 显示参数型调整是否免费；
- [ ] M6-23 内容型创作前说明会生成新的一张；
- [ ] M6-24 完成保存成功和保存失败状态。

### 11.5 响应式与可访问性

- [ ] M6-25 桌面端完整可用；
- [ ] M6-26 手机端页面不横向溢出；
- [ ] M6-27 所有表单有明确标签；
- [ ] M6-28 状态不只依赖颜色表达；
- [ ] M6-29 键盘可以完成主要操作；
- [ ] M6-30 错误提示靠近发生问题的控件。

### 11.6 端到端测试

- 上传合法图片并生成；
- 上传损坏文件；
- 主体不确定后重新选择；
- 风险提示后继续；
- 剩余张数不足；
- 生成中刷新页面；
- 自动重试后成功；
- 生成失败后张数释放；
- 保存失败后重试保存；
- 手机宽度完成基础生成。

### 11.7 完成标准

- PRD 的 22 个页面状态全部可触发和验证；
- 用户不用理解后台模型也能完成流程；
- 错误、权限和张数文案保持一致；
- 完成第一次完整用户可用性走查。

### 11.8 MVP 节点

M6 完成后形成可邀请少量测试者体验的 MVP。

---

## 12. M7：Plus/Pro 高级创作

### 12.1 目标

实现由后台权限控制的高级图像创作、二次创作和版本管理。

### 12.2 高级模型选型

- [ ] M7-01 选择 2～3 个图像编辑或生成候选模型；
- [ ] M7-02 比较主体忠实度、局部编辑、中文指令、延迟、成本和安全；
- [ ] M7-03 优先选择能够保留人物或宠物身份的模型；
- [ ] M7-04 配置首选和降级模型；
- [ ] M7-05 保留模拟模型用于本地测试。

### 12.3 创作能力

- [ ] M7-06 创建高级创作 Prompt v1.0；
- [ ] M7-07 区分必须保留和允许修改的内容；
- [ ] M7-08 实现风格转换；
- [ ] M7-09 实现背景创作；
- [ ] M7-10 实现元素增删；
- [ ] M7-11 实现主体轮廓强化；
- [ ] M7-12 实现局部编辑区域；
- [ ] M7-13 将创作底图接入统一图纸转换；
- [ ] M7-14 高级模型不可用时禁止错误扣减和错误降级。

### 12.4 二次创作和版本

- [ ] M7-15 区分参数型和内容型修改；
- [ ] M7-16 参数型修改复用已有底图；
- [ ] M7-17 内容型修改生成新作品版本；
- [ ] M7-18 保存父版本关系；
- [ ] M7-19 允许查看旧版本；
- [ ] M7-20 内容型创作成功后使用一张；
- [ ] M7-21 失败时释放预留并保留旧版本。

### 12.5 视觉复查

- [ ] M7-22 创建结果复查 Prompt v1.0；
- [ ] M7-23 检查主体、关键特征、背景和用户要求；
- [ ] M7-24 将结果划分为通过、警告、重试或失败；
- [ ] M7-25 自动重试最多一次；
- [ ] M7-26 保存视觉复查结论。

### 12.6 必须编写的测试

- 未授权会员不能使用高级能力；
- 权限修改后新任务按新配置执行；
- 必须保留的主体没有被任意替换；
- 内容型修改形成新版本；
- 参数型修改不使用新张数；
- 高级生成失败不破坏原版本；
- 自动重试只执行一次；
- 高级模型故障不影响基础生成。

### 12.7 完成标准

- 至少有三类高级创作能力可以稳定演示；
- 高级结果仍通过统一网格、色板和数量校验；
- 作品版本和张数记录一致；
- Plus/Pro 权限完全来自后台配置。

---

## 13. M8：图纸库、PNG/PDF 与运营后台

### 13.1 图纸库

- [ ] M8-01 完成“我的图纸”列表；
- [ ] M8-02 支持查看作品详情；
- [ ] M8-03 支持查看版本历史；
- [ ] M8-04 支持重命名和备注；
- [ ] M8-05 支持从旧版本继续创作；
- [ ] M8-06 处理删除与资源清理策略；
- [ ] M8-07 确保作品只对所属用户可见。

### 13.2 PNG 输出

- [ ] M8-08 导出效果图 PNG；
- [ ] M8-09 导出带网格和颜色编号 PNG；
- [ ] M8-10 不同尺寸下文字和网格可读；
- [ ] M8-11 导出文件与正式图纸版本关联。

### 13.3 PDF 制作图

- [ ] M8-12 设计 PDF 页面结构；
- [ ] M8-13 输出作品信息和完整预览；
- [ ] M8-14 输出分页网格和坐标；
- [ ] M8-15 输出颜色编号和材料清单；
- [ ] M8-16 输出制作建议；
- [ ] M8-17 生成后进行页面渲染检查；
- [ ] M8-18 处理中文字体嵌入和打印清晰度。

### 13.4 运营后台

- [ ] M8-19 管理会员名称、周期张数和权限；
- [ ] M8-20 管理图纸尺寸、颜色档位和色板；
- [ ] M8-21 管理模型路由、启用状态和 Prompt 版本；
- [ ] M8-22 管理质量阈值和重试次数；
- [ ] M8-23 查看任务、失败和重试；
- [ ] M8-24 查看生成张数预留、使用和释放；
- [ ] M8-25 查看模型耗时和后台成本；
- [ ] M8-26 保存配置变更记录；
- [ ] M8-27 确保运行中任务使用配置快照。

### 13.5 必须编写的测试

- PNG 与正式网格一致；
- PDF 材料数量与数据库一致；
- 30×30、50×50、70×70 PDF 均可阅读；
- 中文没有乱码或缺字；
- 普通用户无法进入运营后台；
- 配置变化不修改运行中的任务；
- 管理员额度调整保留审计记录。

### 13.6 完成标准

- 用户可以在线查看并导出 PNG 和 PDF；
- PDF 经过实际渲染检查；
- 管理员无需改代码即可调整 PRD 指定的配置项；
- 后台可以解释一次任务使用了什么模型、Prompt、配置和张数记录。

---

## 14. M9：全量测试、优化和演示发布

### 14.1 测试数据准备

- [ ] M9-01 准备约 40 张合法测试图片；
- [ ] M9-02 按人物、宠物、物品、插画和特殊图片分类；
- [ ] M9-03 为每张图片记录预期主体和风险；
- [ ] M9-04 固定测试参数组合；
- [ ] M9-05 建立测试结果记录表。

### 14.2 自动测试

- [ ] M9-06 运行全部单元测试；
- [ ] M9-07 运行数据库和任务集成测试；
- [ ] M9-08 运行生成张数并发测试；
- [ ] M9-09 运行模型失败和降级测试；
- [ ] M9-10 运行六步端到端测试；
- [ ] M9-11 运行用户数据隔离测试；
- [ ] M9-12 运行 PNG/PDF 输出测试。

### 14.3 人工质量验收

- [ ] M9-13 对 40 张图片生成正式结果；
- [ ] M9-14 记录主体可辨认性；
- [ ] M9-15 记录制作可行性；
- [ ] M9-16 检查材料清单；
- [ ] M9-17 检查高级创作符合度；
- [ ] M9-18 对少量图纸进行真实拼豆制作验证；
- [ ] M9-19 形成测试报告。

### 14.4 性能与稳定性

- [ ] M9-20 检查上传、分析、生成和导出耗时；
- [ ] M9-21 检查多个任务同时运行；
- [ ] M9-22 检查任务工作进程重启后的恢复；
- [ ] M9-23 检查外部模型超时和限流；
- [ ] M9-24 检查无效文件和超大文件；
- [ ] M9-25 检查日志中不暴露密钥和敏感地址。

### 14.5 作品集演示准备

- [ ] M9-26 准备游客、注册、Plus、Pro 演示账号；
- [ ] M9-27 准备三组稳定的演示图片；
- [ ] M9-28 准备一次正常流程和一次异常恢复演示；
- [ ] M9-29 在产品中展示关键产品决策；
- [ ] M9-30 整理架构图和开发复盘；
- [ ] M9-31 编写最终 README；
- [ ] M9-32 部署 Demo 环境；
- [ ] M9-33 完成上线后冒烟测试。

### 14.6 发布门槛

必须同时满足：

- 技术数据一致性为 100%；
- 图纸文件生成成功率至少 95%；
- 主体可辨认通过率至少 85%；
- 严重主体错误率低于 5%；
- 自动重试率低于 15%；
- 可制作比例至少 85%；
- 系统失败错误扣减张数为 0；
- P0 和 P1 问题全部关闭；
- 关键自动测试全部通过；
- 部署环境完成冒烟测试。

---

## 15. 关键依赖和执行顺序

### 15.1 关键路径

```text
M0 工程基础
  ↓
M1 拼豆核心
  ↓
M2 数据与配置
  ↓
M3 基础闭环
  ↓
M4 任务和张数
  ↓
M5 AI 分析
  ↓
M6 完整体验
  ↓
M7 高级创作
  ↓
M8 导出和后台
  ↓
M9 验收发布
```

### 15.2 不应提前做的工作

- M1 完成前，不开始 Pro 图像创作；
- M2 数据模型稳定前，不制作复杂运营后台；
- M3 基础闭环完成前，不优化大量视觉细节；
- M4 额度事务测试通过前，不开放多任务并发；
- M5 模型比较完成前，不把供应商写入页面逻辑；
- M6 完成前，不增加 PRD 之外的新功能；
- M8 PDF 渲染检查通过前，不宣称图纸可以打印制作；
- M9 验收通过前，不进行正式演示发布。

---

## 16. 测试策略

### 16.1 测试金字塔

1. **单元测试最多**：颜色映射、网格、材料统计、状态和额度规则。
2. **集成测试其次**：数据库、任务队列、模型适配、文件保存和导出。
3. **端到端测试少而关键**：六步用户流程及主要异常。
4. **人工视觉测试固定样本**：判断主体和制作体验。

### 16.2 每个功能的完成定义

一个任务只有同时满足以下条件才可以标记完成：

- 代码已经实现；
- 正常流程测试通过；
- 至少一个主要异常测试通过；
- 页面文案和错误状态已补充；
- 没有硬编码密钥和业务配置；
- 相关文档已更新；
- 代码检查和全部相关测试通过；
- 已完成一个原子 Git 提交。

### 16.3 回归规则

- 修改图纸算法：重新运行全部网格和材料测试；
- 修改色板：重新验证所有颜色合法性和材料清单；
- 修改 Prompt 或模型：使用固定测试图片重新比较；
- 修改额度逻辑：运行并发、失败、重试和幂等测试；
- 修改 PDF：重新渲染三个尺寸并人工查看；
- 修改会员配置：检查四种用户层级的权限。

---

## 17. 风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| 一开始沉迷 AI 效果 | 核心图纸不可制作 | M1 先完成确定性核心，AI 后接 |
| 颜色映射视觉效果差 | 主体难辨认 | 使用色差算法、关键区域保护和固定测试集 |
| 背景分割不稳定 | 主体边缘异常 | 分割按需调用，失败时允许保留背景 |
| 模型输出字段不稳定 | 页面和任务失败 | 固定结构、字段校验、一次重试和规则降级 |
| 额度并发错误 | 用户权益受损 | 数据库事务、幂等键、并发自动测试 |
| 后台任务难以恢复 | 用户卡在生成中 | 持久化状态、阶段日志和恢复任务 |
| PDF 网格不可读 | 无法实际制作 | 三种尺寸渲染检查和实际打印测试 |
| 高级模型成本过高 | Demo 使用受限 | 适配层、模型路由、缓存和调用记录 |
| 功能范围不断扩大 | 无法按期完成 | 以 PRD v1.0 和发布门槛控制范围 |
| 初学者一次接触太多技术 | 学习和开发停滞 | 每阶段只学习当前必需概念并形成复盘 |

---

## 18. 项目驱动学习路线

每个里程碑只要求掌握当期需要的知识。

| 阶段 | 需要学习的核心内容 |
|---|---|
| M0 | Python 环境、Django 基础、Git、测试命令 |
| M1 | 函数、列表和字典、NumPy 数组、图片像素、单元测试 |
| M2 | 数据库表、关系、迁移、用户和权限 |
| M3 | 请求、表单、模板、文件上传、服务分层 |
| M4 | 后台任务、状态机、事务、幂等和并发基础 |
| M5 | API 调用、结构化输出、错误处理、Prompt 版本 |
| M6 | 页面状态、HTMX、基本 JavaScript、端到端测试 |
| M7 | 图像编辑、蒙版、版本关系和高级权限 |
| M8 | 文件导出、PDF、后台配置和运营数据 |
| M9 | 测试报告、部署、监控和项目复盘 |

建议每完成一个里程碑记录：

- 这一阶段解决了什么用户问题；
- 使用了哪些新概念；
- 遇到的最重要问题；
- 为什么采用当前方案；
- 如果重新开发会怎样改进。

这些记录可以直接转化为求职作品集内容。

---

## 19. Git 和任务管理规则

### 19.1 分支建议

- `main`：始终保持可运行；
- 每个小任务使用短期功能分支；
- 一个分支只解决一个清楚的问题；
- 合并前必须通过相关测试。

### 19.2 提交规则

- 每次提交一个原子变更；
- 提交信息使用英文；
- 使用 Conventional Commits；
- 不把格式化、重构和新功能混在同一提交；
- 不提交密钥、用户图片和大型临时输出。

常用格式：

```text
feat: add bead palette model
fix: release quota after failed generation
test: cover concurrent quota reservation
docs: update ai workflow architecture
chore: configure linting
```

### 19.3 任务卡必须包含

- 用户或系统目标；
- 允许修改的范围；
- 依赖任务；
- 验收条件；
- 必须测试的正常和异常情况；
- 是否涉及数据迁移和配置变化。

---

## 20. 首批可直接执行的任务

开发开始后严格按以下顺序执行，不同时启动多个大阶段。

### 第 1 批：工程可运行

1. M0-01 初始化 Git 仓库；
2. M0-02 建立 Python 环境和依赖清单；
3. M0-03 创建 Django 项目；
4. M0-05 配置 PostgreSQL；
5. M0-06 配置 Redis 和 Celery；
6. M0-08 配置 Ruff 和 pytest；
7. M0-10 创建健康检查及测试；
8. M0-11 编写启动文档。

### 第 2 批：证明拼豆核心可行

1. M1-01 定义通用色板格式；
2. M1-03 定义正式网格结构；
3. M1-07 读取并标准化图片；
4. M1-09 正方形裁剪；
5. M1-10 网格缩放；
6. M1-11 色板匹配；
7. M1-13 材料统计；
8. M1-14/M1-15 PNG 预览；
9. M1-17 技术校验；
10. 完成全部 M1 测试。

### 第 3 批：建立第一个用户闭环

M1 验收通过后，按 M2 数据模型、M3 上传页面、M3 图纸结果页和保存功能的顺序执行。

---

## 21. 版本发布节点

### Prototype：M1 完成

证明图片可以转换为准确网格和材料数据，不要求完整页面。

### Internal Alpha：M3 完成

注册用户可以完成基础上传、生成、预览和保存。

### MVP Beta：M6 完成

六步流程、AI 分析、后台任务、生成张数和异常状态完整。

### Portfolio v1.0：M9 完成

包含高级创作、图纸库、导出、运营后台、测试报告和部署演示。

---

## 22. 当前不阻塞开发的可配置项

以下内容不需要在开始开发前最终确定：

- 游客、注册、Plus 和 Pro 的具体价格；
- 各层级周期生成张数；
- Plus 和 Pro 的最终功能组合；
- 具体模型供应商；
- 模型的真实单次价格；
- 后续品牌色板；
- 后续新增尺寸；
- Excel、SVG 等扩展输出。

开发时只需确保这些内容通过配置层读取，并有合理的演示默认值。

---

## 23. 开始开发前的最终检查

- [ ] `prd.md` 保持为已确认需求基线；
- [ ] 本开发计划得到确认；
- [ ] 默认技术方案得到确认或完成调整；
- [ ] 本地具备 Python、数据库和 Redis 运行环境；
- [ ] 测试图片具有合法使用权；
- [ ] 确定代码仓库和部署目标；
- [ ] 从 M0-01 开始，不提前进入高级功能。

只要上述项目完成，即可直接进入 M0 工程初始化。

